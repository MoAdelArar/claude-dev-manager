Show the details of a specific artifact or feature.

Use the cdm_show_artifact tool with:
- projectPath: the current project's absolute root path
- artifactId: "$ARGUMENTS"

Display the full artifact content including type, status, creator, and version.
