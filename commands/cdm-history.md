Show the development history for the current project.

Use the cdm_get_history tool with the current project's absolute root path.

If the user specifies a feature name or ID, filter the history to that feature using the featureId parameter.

Present the results showing:
1. Summary metrics (features, stages, artifacts, issues, tokens, duration)
2. Agent activity breakdown
3. Recent timeline events

If no history exists yet, suggest running /cdm-start to generate tracked events.
