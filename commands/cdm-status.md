Show the current status of all CDM features and their pipeline progress.

Use the cdm_get_status tool with the current project's absolute root path.

Present the results in a clear summary showing each feature's name, status, current stage, artifact count, and issue count.
